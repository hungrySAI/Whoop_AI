# WHOOP Personal Copilot 开发约定

本项目由用户于 2026-09-05 授权从审查材料新建，首批范围是 F0 合成数据基础闭环。
本文件规范后续开发；用户在会话中明确提供的授权、修订和范围优先。
用户随后批准 F1 官方导出与 OAuth 接入；当前实现及真实联调缺口见 STATUS.md。
后续已批准 F2 本地看板、F2.1 覆盖说明、F2.2 按需更新及 F2.3 官方 Journal 导出只读时间线。
F2.3 代码与合成验收完成，实际 Journal 文件与映射联调仍待提供导出；不将其记作真实导入完成。
用户随后明确否定将定期手工导出/导入 Journal 作为长期流程：该能力降为可选历史补充，
真实导出验收暂缓且不阻塞主线，不再要求用户下载或定期维护文件。
日常功能优先使用官方 API 可自动获取的数据；不能用文件夹监控冒充端到端 Journal 自动接入。
用户随后批准 F2.4：本地每日摘要与近期变化。复用现有官方三项概览和 mean_change/1，
各项日期独立保留，样本不足不输出变化结论，未结束周期明确标注；不依赖 Journal 或增加日常输入。
用户继续批准下一步，F2.5 增加本地周回顾：复用六项指标与注册均值统计，默认上个完整 UTC 日历周，
本周只显示进度；来源详情、修正/删除/到期沿用既有服务。不新增日常输入、永久报告副本或后台任务。
用户继续要求下一步，F2.6 推进本地恢复回看：仅按官方周期和睡眠 ID 关联当前保留记录，
复用已有规范观测、评分状态及来源详情；不推断因果、相邻周期或运动归属，不增加用户日常输入。
2026-09-07 用户批准 F2 本地 Dashboard 第一版：官方概览、7/30 天趋势、来源详情、手动同步与连接状态。
浏览器只在本机显示获准的真实数据，继续沿用共享 Python 服务和同一加密数据库。
用户随后批准 F2.1：区分请求范围与记录覆盖、完善空态，并用合成数据完成浏览器交互及窄屏验收。
不得用记录起止日期推断精确佩戴起点，也不得把无训练记录日期认定为漏同步。
用户随后批准 F2.2：数据新鲜度、打开看板时按需更新及失败恢复。
每次打开最多发起一次条件检查；阈值为本应用的 30 分钟策略，已有未完成断点不自动重试。
沿用官方 API 客户端和共享同步服务，不新增定时任务、云资源或真实健康数据外发。

2026-09-07 用户明确批准 F3.1「启动体验与长间隔补取」，暂缓后台定时同步。
一键启动/停止仅管理本机按需服务；不安装登录项、定时任务或云资源。补取由共享 SyncService
在锁内合并当前保留的成功请求区间，回查起点附近 7 个 UTC 日历日，自动范围最多 366 天；
无可用成功起点（含记录到期）时进行同上限的回查，并明确更早历史未核查。
该查询上限独立于按采集时间计算的存储保留期限；未完成断点仍不自动重试。

2026-09-07 用户批准 F3.2「第一版使用验收与恢复演练」，并反馈本批周回顾来源记录、
恢复回看周期/睡眠关联与 WHOOP App 核对一致；不扩大为全部历史逐条对账。
恢复演练只在临时隔离副本中执行，真实数据继续使用 SQLCipher 与项目 Keychain，输出仅技术结果。
发现并修复恢复副本继承原库受管备份登记的问题：恢复目标不继承原库清理权限，只管理自身新建备份。
演练完成后清理本次临时副本、对应登记及临时 Keychain 项；不得以同机恢复声称跨设备灾难恢复完成。

2026-09-07 用户在独立数据稳健性审查后批准修复加固（F3.3）。0.4.2 / schema 3
修复到期与去重竞态、分析删除后重写、UUID 别名、未来同步窗口、页面缓存及查询规模问题。
日常视图复用注册计算但不保存分析副本；显式分析继续保留证据。到期清理与入库共用写事务；
当前来源版本到期时一并清理同身份前驱，防止旧修正/删除状态回退，不延长保留期。
迁移保留源版本 ID、原始哈希与原文，独立规范身份键用于去重；受身份合并影响的旧分析需重算。
历史修正轮查及上游删除完整传播仍是独立能力边界，不把本批六项加固声称为完整历史对账。

2026-09-07 用户确认 V1 已完成，授权整理本地项目并创建 GitHub `Whoop_AI` 仓库安全上传。
本次以私有代码仓库交付，范围限源码、锁文件、合成测试/样例、项目文档与技术验收证据。
真实运行数据、数据库、备份、原始导出、凭据及本机配置不进入 GitHub；本地使用方式保持不变。
V1 为产品里程碑，技术版本保持 0.4.2 / schema 3。历史审查归档不改变原有授权边界。

## 权威位置

- 产品定位与本批范围：`PROJECT_CHARTER.md`。
- 当前架构和数据语义：`docs/ARCHITECTURE.md`；决策：`docs/DECISIONS.md`。
- 官方资源核查：`docs/WHOOP_RESOURCES.md`。
- 实际验证与缺口：`STATUS.md`、`docs/ACCEPTANCE.md`；运行方式：`README.md`。
- 文档导航：`docs/README.md`；日常操作：`docs/DASHBOARD.md`。
- `reviews/2026-09-05/` 中的 `ARCHITECTURE_REVIEW.md`、`FOUNDATION_REVISION_SPEC.md`、
  `CODEX_REVISE_FOUNDATION.md` 及原 README 是历史审查与设计来源，不是已实现功能证明，
  也不自动扩大当前会话的动工授权。后续独立审查按日期保存在 `reviews/`。

## 实现原则

- 首先核查 WHOOP 官方 API / OpenAPI、导出和现有工具；复用已有指标、协议和基础库。
- 保留 WHOOP 来源与官方评分语义；均值等本应用派生值必须清楚标记。
- 数值由注册计算函数产生。CLI / MCP 调用同一应用服务，不复制业务规则。
- 数据提供方与内部 subject 分离；原始版本、规范值、派生证据相互可追溯。
- 测量时间、来源更新时间、获知时间分开；不把缺失或未评分数据填成零。
- 默认环境保持合成。真实数据只进入经本地操作者明确授权的 SQLCipher 数据库，密钥及 OAuth 凭据在 OS Keychain；不得回退明文或混用环境。
- 官方导出表头未有完整公开契约，先检查实际文件再显式映射列、单位、身份与时区。合成标记不是实际数据来源检测器。
- 模型可查询、按用户请求建草稿；提交必须由应用服务验证具体动作、版本与授权。
- MCP 不提供签发审批、任意 SQL、Shell 或任意文件读取工具。CLI 是可信本地操作者入口。
- 一个 Python 代码库、一套 SQLite 持久层；扩展按实际需求增加适配器、指标或命令。
- F1 提供本地 OAuth 与六种只读资源的同步入口，实际登录由用户创建应用后主动发起。真实数据外发到模型、通知、公开端点或云资源不在当前授权内；MCP 仍只开放合成数据库。
- F2 HTTP 只绑定 127.0.0.1，校验 Host/Origin、会话和同步 CSRF；前端资产本地提供。
  不通过截图、DOM 或工具输出把真实健康内容交给模型；验收记录只保存技术检查状态。
- 保留期限、源删除、派生缓存和受管备份清理必须保持一致；不能将自行复制的外部备份宣称纳入自动清理。

## 开发与验证

在本项目内工作，保留用户改动，不搜索其他个人目录寻找凭据。
依赖使用 `uv sync --locked` 安装到 `.venv`，不修改系统环境。运行 `uv run pytest -q`
及 `uv run ruff check .`；涉及修改的行为需要有相应验证，未测项目明确写 `not_run`。
修改源版本排序、动作授权、幂等、任务租约或恢复逻辑时，补充对应回归测试。
保持合成 fixtures 可入 Git；真实运行数据、数据库、秘密和本地凭据不得入 Git。
