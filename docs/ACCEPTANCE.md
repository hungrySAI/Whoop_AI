# V1 验收与缺口

日期：2026-09-07。此表对照 [原基础修订规格](../reviews/2026-09-05/FOUNDATION_REVISION_SPEC.md) 的 X01—X15；
`passed (F0)` 仅表示所述合成范围通过。F1 自动化测试以合成内容验证，另有首次真实小窗口联调，二者均不能外推生产安全或完整历史覆盖。
测试实际总数和运行证据见 [STATUS.md](../STATUS.md)。

| 编号 | 状态 | 当前证据 / 仍缺能力 |
|---|---|---|
| X01 第二来源解耦 | passed (F0) | WHOOP 与手工 CSV 使用同一输入契约；新增来源不修改 WHOOP 解析路径 |
| X02 新指标扩展 | passed (F0) | 测试注册第三方身高指标，复用同一存储与分析入口，无 schema 变更 |
| X03 入口一致 | passed (covered metrics) | CLI / MCP 与 F2 六指标、两窗口的计算/证据一致；Web 只返回精简视图，F2.1 合成浏览器交互验收通过 |
| X04 修正与复算 | passed (F0) | 源修正使旧当前结果 stale；原输入版本仍可复算，worker 计算新结果 |
| X05 当时已知历史 | passed (F0) | as_of 不读后来获知的修正；首次记录前返回历史不足 |
| X06 写入范围与授权 | passed (F0) | 仅本地训练计划命令；缺合法审批不能提交；依赖可信本地操作者权限 |
| X07 审批绑定 | passed (F0) | 内容/目标/版本/scope/期限绑定；篡改、过期、撤销、并发版本冲突拒绝 |
| X08 幂等与恢复 | passed (F0) | 重复导入、重复提交、双 worker、双查询发布、重启后租约过期恢复；无外发动作 |
| X09 外发撤销 | not_run | 没有外发授权与发送通道，不能用本地动作撤销代替该项 |
| X10 保留与删除传播 | partial (F1) | 本地到期/来源清除、规范值/分析/任务/暂存与受管备份传播已测；独立副本、系统快照与尚未实现的会话不在覆盖内 |
| X11 模型解耦 | partial | 无模型即可运行全部 F0 功能；尚无模型适配器，替换/回退未测试 |
| X12 探索与事实分离 | not_run | 当前仅注册描述统计与计划草稿；未实现模型假设/探索分析/事实存储 |
| X13 上下文过期 | not_run | 目标、限制与会话上下文管理未实现 |
| X14 Git 数据隔离 | passed (F0) | 修订根目录匹配；15 项实际 Git 检查，包含源码/fixtures/配置样例及秘密/运行数据 |
| X15 真实数据保护 | partial (F1) | 授权门禁、SQLCipher/WAL/备份、固定 HTTPS 及期限逻辑通过合成验证；另通过实际 Keychain、六资源入库、加密库完整性与本地复算，生产恢复仍 not_run |

## F1 接入专项

| 能力 | 验证与边界 |
|---|---|
| 官方导出复用 | `test_exports.py`：CSV/ZIP、显式表头与单位、缺失值、稳定身份及时间修正、夏令时、路径/大小/解压比限制；实际官方导出仍 not_run |
| OAuth / OS Keychain 接口 | `test_oauth.py`：Authlib 模拟交换、state、回调匹配、刷新轮换、并发锁、丢失响应、撤销、安全输出；Keychain 使用测试后端，未触发实际凭据访问 |
| WHOOP HTTP | `test_whoop_client.py`：固定端点、401 单次刷新、429/5xx 有界等待、分页循环/预算、请求不继承环境代理或跟随跳转 |
| 六资源规范化 | `test_sync.py`：固定 OpenAPI 契约；复杂睡眠原文保留；关联周期补取；未知测量时间不补造；同名周期/训练指标必须区分 |
| 持久同步 | 页面/游标一致保存、失败不发布部分领域记录、重开续传、入库后中断重放去重；合成 HTTP → SQLCipher 跨层验证 |
| 加密与保留 | `test_real_storage.py`：普通 SQLite/错误密钥拒读，数据库/WAL 不出现已知明文，加密备份与换密钥恢复，到期拒绝复活，替换过的受管备份不误删 |
| 采集/获知时间 | 先采集后入库的期限不延后；重复采集不重置原版本，未来/过期采集拒绝，过期暂存不能继续同步 |
| CLI / 包分发 | `test_f1_cli.py`：导出检查/去重，真实存储授权早于钥匙串访问，默认合成禁止网络同步；实际 wheel 包含原样 OpenAPI |
| F0 兼容 | 原 89 项继续通过；schema 1 → 2 迁移测试及真实 F0 合成数据库的 28 个版本和原分析复算通过 |

API 标量规范化覆盖恢复分、HRV、RHR、血氧、皮温、负荷、平均/最大心率、能量、睡眠表现/效率/一致性、呼吸率。
profile/body 完整保存原始响应；睡眠阶段、需求、训练心率区间等复杂字段保留原文，尚无完整规范查询或教练分析。
当前合计 **483 passed**（含下述 F2.6）；初版依赖与 254 项结果见 [F1_VALIDATION.json](../evidence/F1_VALIDATION.json)，
新增回归及真实联调见 [F1_LIVE_VALIDATION_2026-09-07.json](../evidence/F1_LIVE_VALIDATION_2026-09-07.json)。

2026-09-07 已确认首次 OAuth、当前回调接受、六项读取 scope + offline，以及实际 Keychain 访问。
真实六资源窗口经过中断后续传完成，SQLCipher 完整性、外键、保留期限、完成后无网络重放和本地分析复算通过。
WorkoutScore 的三个可选距离/海拔字段返回 null，已用完全合成的样例补充续传、原值保留和严格类型回归。
随后 F2 已通过真实刷新与 30 天请求窗口；真实撤销、实际导出映射、完整历史及逐字段对账、真实备份恢复演练仍为 `not_run`。

## F2 本地 Dashboard 专项

`tests/test_dashboard.py` 新增 55 项，全部使用合成记录、临时库和测试密钥。

| 能力 | 验证与边界 |
|---|---|
| 注册统计一致 | 六指标 × 7/30 天，与 CopilotService 的结果及分析 ID 一致，原始证据可复算 |
| 官方语义 | 最新未评分/无法评分/校准不回退旧值；缺字段/无记录不填零；同日多训练不折为官方日均；周期/训练分开 |
| 来源 | 测量、记录更新、关联周期更新、采集、获知时间分开；修订/旧响应/删除/到期和查询期间清理回归通过 |
| 本机 HTTP | 精确 Host/Origin/Sec-Fetch-Site、非回环拒绝、会话/CSRF、2 KB 输入上限、无通用文件接口、安全响应头及异常脱敏通过 |
| 真实库隔离 | 模拟真实库经 HTTP 查询；SQLCipher 保持加密，无明文副本；演示或非法端口先于真实库/Keychain 访问拒绝 |
| 手动同步 | 线程独立 Store、重复点击去重、故障脱敏、保存断点后继续原请求窗口通过 |
| 包分发 | 0.3.0 wheel 包含本地资产、Chart.js MIT/来源/哈希、合成 fixture 与未改动官方 OpenAPI，无运行文件 |
| 真实 HTTP | 本机六指标 × 两窗口、来源详情/统计一致、手动六资源同步完成；只输出技术状态，健康内容留在本机 |
| 浏览器交互/视觉 | F2.1 passed（合成）；原 F2 此项为 not_run，历史证据保留；未读取真实 DOM/截图 |

记录见 [F2_VALIDATION_2026-09-07.json](../evidence/F2_VALIDATION_2026-09-07.json)。
看板只提供第一版获准功能，不包含训练计划界面、模型、WebMCP 真实数据、远程访问或云部署。

## F2.1 覆盖与可用性

新增 8 项自动化回归验证短历史、同日多记录、各无有效评分状态、空窗口、失败保留旧数据与半开窗口终点。
浏览器使用 `tests/dashboard_scenarios.py` 的临时合成库，在独立本机端口逐一检查 8 类场景：
短历史、等待评分、无法评分、校准中、未提供指标、无记录、一次同步失败后续传、状态接口暂时失败后恢复。
窗口切换、来源详情/原值、Escape 关闭、同步进度与原断点窗口保留通过。
桌面和窄屏实测 CSS 视口为 984×738 与 390×844，截图/DOM 检查未发现页面水平溢出；
窄屏均值与变化区采用上下排列。其他浏览器、完整 WCAG/辅助技术与全部真实历史逐条对账为 not_run。
验收过程只查看合成内容，没有触发真实 WHOOP 同步或读取真实浏览器页面。
用户随后完成本机 WHOOP App 抽查并回复“一致”。此项记为用户确认的抽查通过，
不作为代理独立读取真实页面或全部历史逐条对账的证明；未收集具体样本日期、数量或健康值。
验证见 [F2_1_VALIDATION_2026-09-07.json](../evidence/F2_1_VALIDATION_2026-09-07.json)。

## 关键行为验证

`tests/test_adapters.py`：官方字段与评分状态、cycle 时间归属、可选缺失值、校准、
无时区/非法单位/非有限数值、外部用户混合、CSV 原始值保留及 kg/lb 转换。

`tests/test_foundation.py`：来源去重、规范存储、均值/差值、修正失效、历史查询、
WHOOP 组合版本防倒退、来源删除标记、独立身份、事务回滚、新指标、多源不静默合并、
任务租约和并发、分析并发去重、重启、备份恢复及拒绝不匹配数据库。

`tests/test_commands.py`：审批哈希持久化、内容/目标篡改、过期/撤销、幂等键冲突、
重复提交原结果、重开连接、并发版本冲突与事务回滚。

`tests/test_mcp.py`：官方 SDK 客户端实际启动本地 stdio 服务；握手、工具列表、
CLI/MCP 同输出、证据复算、错误输入、禁止签发审批、伪 token 拒绝、合法提交与重放。
这不是 Codex 桌面实际客户端联调。

`tests/test_cli.py`：演示可重复、训练草稿不自动提交、非交互审批必须提供精确的已审阅哈希、
错误返回非零状态与结构化信息。

## 本轮不作的推定

版本化存储不代表真实数据永久保留获准；文件权限不代表静态加密。
合成字段与单元测试不代表官方账号实际覆盖率。
本地 CLI 是操作者权限边界，不防御拥有相同 shell/数据库写权限的进程。
未实现项目保持独立状态；原始审查的 47 项测试未包含在本次提供的材料中，不能虚报回归通过。

## F2.2 数据新鲜度与日常恢复

`test_dashboard_freshness.py` 新增 23 项；全套 346 项通过，Ruff、格式、JS 语法、锁文件、wheel 完整性通过。
覆盖短历史/待评分/校准/空数据、新鲜度阈值与 UTC 跨日、7/30 天请求匹配、旧请求延迟完成、
跨 Runtime 锁与持久状态复查、网络/客户端构造/页数暂停、显式续传与重同步、授权恢复、CSRF/输入验证、
无暂存页断点到期/受管备份失效/备份拒绝复活、时钟回拨及另一进程成功后清除旧错误。

合成浏览器四类场景：`stale`、`stale-failure`、`authorization-required`、`window-unavailable`。
检查打开时新请求、刷新复用、切换窗口不自动采集、续传保留原窗口、授权禁用、旧视图保持和失败回退一致。
桌面 984×738、窄屏 390×844 检查通过；未见横向溢出；窗口故障场景预期 HTTP 400。
本轮未等待真实 30 分钟观察前端提示到期，服务端阈值边界已有合成时钟回归。

真实本机 0.3.2 打开时条件同步已完成；只验证技术状态和本地 HTTP 契约，不读真实 DOM/截图，
不输出健康值、账户身份、请求范围或断点 ID。真实撤销/故障注入和完整历史对账仍 not_run。
详见 [F2_2_VALIDATION_2026-09-07.json](../evidence/F2_2_VALIDATION_2026-09-07.json)。

## F2.3 Journal 导出与只读时间线

`tests/test_journal.py` 增加 36 项，全套 382 passed。使用虚构问答、临时库、测试密钥和模拟 API。

| 能力 | 验证与边界 |
|---|---|
| 复用导入 | 显式长/宽表、CSV/ZIP 一致、v1 保留但不猜测投影；既有 CLI 临时库首次 5 条、重复去重 5 条 |
| 日期语义 | 仅日期不生成观测/活动；日历标签、+14 偏移、显式偏移优先、23/25 小时日及缺失午夜通过；含糊/不存在时间拒绝 |
| 映射身份 | 必须确认本地 subject，回答不得用于稳定身份；不宣称 OAuth 验证导出账户 |
| 当前版本 | 重复/修正/后来旧快照、旧证据失效、来源清除/到期、分页清理后回退、查询期间删除重读通过 |
| 官方并列 | 复用已有 API 视图按当地日界半开范围筛选，最新未评分保留、不借用旧分数，不进行因果计算 |
| 隐私与保留 | HTTP 不返回原行/身份/路径；既有会话、同源、预算、no-store；SQLCipher 到期和受管备份传播通过 |
| 浏览器 | 5 类合成场景：journal、journal-pages、journal-empty、journal-legacy、journal-unavailable；7/30 切换、20/5 分页、原文与空白、双类来源/键盘关闭、失败保留 7 天视图及刷新清错通过 |
| 视觉 | 桌面与 390×844 窄屏检查，无横向溢出；导入信息可折叠，问答按日期分组，特殊 HTML 只显示文字 |
| 分发 | 0.3.3 wheel 与 28 个源码/资产文件逐字节一致，OpenAPI、Chart.js 哈希/MIT 与合成 fixture 保留，无运行库和测试目录 |
| 本机升级 | 实际获准加密库中新 Journal 两窗口 HTTP 契约、已有概览与连接状态通过；只返回技术布尔值，不读取真实 DOM/截图 |
| 真实 Journal | **not_run**；尚无实际导出。真实表头、日期依据、稳定身份、本人归属与导入结果仍需文件到达后在本机核对 |

浏览器测试使用临时合成数据库与模拟客户端，结束后停止测试服务、清理测试库、重置视口，
恢复到真实本机看板。故障场景的预期 HTTP 400 不视为正常页面错误；正常场景未见控制台警告/错误。
完整记录见 [F2_3_VALIDATION_2026-09-07.json](../evidence/F2_3_VALIDATION_2026-09-07.json)。
单条编辑/删除、WHOOP 源删除事件、行为影响分析、模型外发和生产环境验收不在本批交付内。

## F2.4 每日摘要与近期变化

`tests/test_briefing.py` 新增 25 项，全套 407 passed。测试仅使用虚构 API 记录、临时库和测试密钥。

| 能力 | 验证与边界 |
|---|---|
| 已有数值复用 | 三指标 × 7/30 天与原 mean_change/1 结果、分析 ID 一致，已有证据可复算；没有新健康计算或 Journal 查询 |
| 样本门控 | 每半段至少 2 条才输出比较；两半各 1 条即使原算法有差值也不作结论；短历史 30 天窗口不填补前半样本 |
| 日期和评分 | 三项不同日期、今日无记录、全空窗口、最新未评分/校准/缺指标保持原义；不推断佩戴或同步故障 |
| 周期与睡眠 | 周期无结束或结束晚于读取截止时明确标注，保留当前官方值；小睡不写成昨夜主睡眠 |
| 变化文案 | 高/低/相同只描述分段均值，百分比用百分点；微小非零差值不显示成零变化 |
| 一致性与保护 | 查询间来源修正、末尾过期清理后整体重读；摘要与卡片源版本/分析 ID 一致；HTTP 沿用会话/no-store，不含原文/身份/Journal 问答 |
| 浏览器交互 | 摘要到对应趋势及键盘焦点、来源打开/ESC、7/30 天、Journal 切换、跨视图请求失败后恢复窗口/日期/新鲜度、同步失败保留摘要与续传通过 |
| 视觉 | 合成桌面与 390×844 窄屏检查，官方值和均值变化并列；无横向溢出；正常场景控制台无错误/警告 |

8 类合成场景：short-history、brief-open-cycle、brief-history、brief-mixed、brief-small-change、
empty、window-unavailable、stale-failure。后两者故意模拟故障，不将预期 HTTP 400 记作正常页面错误。
`stale-failure` 首次等待恢复按钮的工具时间预算到期，随后本地失败状态完成并继续验收；不改变同步逻辑。
未读取真实 DOM/截图；实际本机升级仅核对布尔技术结果。完整范围见
[F2_4_VALIDATION_2026-09-07.json](../evidence/F2_4_VALIDATION_2026-09-07.json)。
摘要不代表因果结论、医疗建议、WHOOP 新评分、独立日报归档、后台通知或模型调用。

## F2.5 本地周回顾

`tests/test_weekly.py` 新增 37 项，全套 **444 passed**；仅使用虚构记录、临时库和测试密钥。

| 验证范围 | 结果 |
|---|---|
| 注册统计 | 六指标完整周复用连续 14 天的 first_half/second_half、精确中点和原 analysis_id，可复算 |
| 固定日历窗口 | 默认上个完整 UTC 周；精确周一日期、最近 12 周、年界/DST、本周截至现在与周一零长度通过 |
| 短历史与评分 | 缺前周、每周一条、无记录、待评分/无法评分/校准/缺指标保留状态；不足样本与本周均不输出比较 |
| 记录与覆盖 | 35 次同日训练保持 35 条/1 个记录日，30/5 分页不重复，删除后末页收敛 |
| 整体一致性 | 跨周一保持选中日期，来源变更/删除和 SQLCipher 到期触发六指标整体更新；明细与总表同次读取 |
| 本机边界 | 会话、参数白名单和重复参数拒绝、Origin/no-store、有限字段投影通过；无新写入 HTTP 接口 |
| 分发与质量 | Ruff lint/format（60 个 Python 文件）、JS/启动脚本语法、锁文件、离线 wheel 及 30 项源代码/资产一致性通过 |
| 实际加密库 | 0.3.5 完整周/本周/记录接口、六项总表与明细一致、样本门控、会话/Origin/no-store、既有路由和连接通过；只输出技术布尔值 |

独立合成浏览器检查 5 类场景：short-history、weekly-complete、weekly-pages、weekly-unavailable、weekly-delayed。
默认周/本周/最早可选周、空态、12 周原生选择、来源弹窗和 Escape、分页、跨视图导航检查通过。
周视图手动同步仍更新最近 30 天并保留选周；该场景只有一次显式触发的模拟客户端调用，选周不发起同步。
模拟窗口失败会恢复原周选择与表格；分页失败保留最后成功页，重试或切指标后清除对应错误。
延迟场景验证报告加载期间来源/选周按钮禁用，离开后旧响应不覆盖概览或 Journal。
桌面 984×738 与窄屏 390×844 检查通过，无页面横向溢出；窄屏明细表在自身容器滚动。
正常场景未见控制台错误/警告；模拟失败的 HTTP 400 为预期。合成服务停止后临时库清理，视口恢复。

真实周回顾 DOM/截图、与 WHOOP App 的本批人工核对、其他浏览器和完整辅助技术认证均为 not_run。
此前 App 抽查反馈不作为本批人工验收；实际 Journal 导出仍为可选暂缓事项。
技术证据见 [F2_5_VALIDATION_2026-09-07.json](../evidence/F2_5_VALIDATION_2026-09-07.json)。

## F2.6 恢复回看

`tests/test_cycles.py` 新增 39 项，全套 **483 passed**。测试使用虚构记录、临时库和测试密钥。

| 验证范围 | 结果 |
|---|---|
| 官方关联 | 以周期/睡眠 ID 及同连接、同账户组合，跨筛选窗口睡眠保留；较晚小睡不替代指定睡眠 |
| 指标复用 | 恢复三项、睡眠四项和周期负荷共八项直接复用规范观测及 `_point`；新增三项睡眠原值与来源一致，六项趋势范围保持原样 |
| 缺失与冲突 | 恢复/睡眠缺失、错误周期或账户、nap 标记、待评分/无法评分/校准/缺指标、内嵌周期版本不同均有独立状态 |
| UUID 生命周期 | 合法大小写按 UUID 同一身份匹配；多别名冲突、删除其中一方后旧别名不得重新出现；来源默认读取仍排除删除 |
| 来源更新与期限 | 修改睡眠、晚到旧版本、删除、SQLCipher 到期及读取中来源变化通过；整页重读保证列表和详情同次读取 |
| 窗口与分页 | 7/30 天周期起点筛选、未结束周期、12 周期分 10/2 页、空态、删除后页码收敛、参数上限及类型拒绝 |
| 本机边界 | 会话/Origin/no-store、参数白名单及重复参数、HTTP 字段投影、无新增写入接口，新增来源指标不成为任意趋势查询 |
| 实际本机 | 0.3.6 两窗口契约与八项来源值/状态/时间一致；已有概览/周回顾/Journal 与连接状态通过；只输出技术布尔值 |
| 分发与质量 | Ruff lint/format（62 个 Python 文件）、JS/启动脚本语法、锁文件、离线 wheel 及 31 个源文件/资产一致性通过 |

实际恢复回看的 DOM/截图、与 WHOOP App 的本批人工核对、其他浏览器和完整辅助技术认证为 not_run。
实际 Journal 导出仍为可选暂缓工作；测试和本地接口检查不替代全部历史逐字段对账。

合成浏览器验收 6 类场景：short-history、cycles-pages、cycles-mixed、cycles-unavailable、cycles-delayed、empty。
检查按周期选行与焦点、7/30 天、10/2 分页、未评分与缺指标、来源弹窗/Escape、关联矛盾、
版本冲突、官方小睡标记、未结束周期、缺失及空态。窗口失败恢复选择/列表/详情，分页失败保留原页；
延迟响应不覆盖周回顾或概览，读取期间列表/来源入口禁用。正常场景控制台无错误/警告，模拟 HTTP 400 为预期。
桌面 984×738 与窄屏 390×844 视觉检查通过，无页面横向溢出；合成服务停止、临时库清理、视口复原。
技术证据见 [F2_6_VALIDATION_2026-09-07.json](../evidence/F2_6_VALIDATION_2026-09-07.json)。

## F3.1 启动体验与长间隔补取

新增 43 项补取测试与 30 项启动器测试，全套 **556 passed**（4.95 秒），Ruff lint/65 文件格式检查通过。
测试仅使用虚构 WHOOP 响应、可控时钟、临时库、测试加密密钥及模拟 Keychain。

- 补取：3/45/120 日离线、366 日上下边界、无/已过期起点、成功空记录、区间相接/嵌套/缺口、
  近期窄请求不能掩盖旧空档、旧断点晚完成、时钟回拨、锁内重算和并发去重通过。
- 恢复与复用：OAuth 工厂失败/网络失败/页预算不自动重试、续传窗口固定、官方修正幂等、
  边界关联周期补读、原采集保留期限未改变通过。
- 启动：配置身份和权限、真实 Keychain 无明文回退、占用端口不打开、脱离终端、重复复用、
  就绪/退出期限、慢响应整体超时、大小/压缩限制、签名停止、忙碌拒绝和一次性停止通过。
- 使用实际独立合成进程完成启动→复用→停止→重复停止，临时服务及数据已清理。
- `node tests/check_dashboard_sync_ui.cjs` 执行真实渲染函数的 6 类合成响应：长间隔、旧空档、
  跨年上限、原断点、周回顾与演示环境；范围文案、按钮及无网络副作用通过。未使用真实浏览器 DOM。
- 真实本机完成新启动并打开浏览器、复用、停止/重复停止/重启，服务保持在原 8766 地址。
  项目 Keychain、连接、计划边界、条件请求、新计划持久化、同步成功/新鲜度和 HTTP 边界通过；
  仅输出技术布尔值，没有健康值、实际查询日期、来源 ID 或凭据。
- 锁文件、项目 .venv 安装、离线 wheel、32 个源文件/资产与官方 OpenAPI 一致性及私有路径排除通过。

Finder 双击手势、本批浏览器交互/视觉、真实长时间离线的逐条历史对账均为 not_run。
请求历史已清理时不能证明任意更早区间完整；366 日上限和近期重查不保证所有旧修正/删除已获取。
后台定时同步明确暂缓，未安装登录项、定时任务、webhook 或云服务；Journal 继续为可选历史补充。
证据见 [F3_1_VALIDATION_2026-09-07.json](../evidence/F3_1_VALIDATION_2026-09-07.json)。

## F3.2 第一版使用验收与恢复演练

当前修复版本 **0.4.1**。全套 **558 passed（5.60 秒）**，Ruff lint/66 个 Python 文件格式、JS/shell、
锁文件和离线构建通过；32 个源文件/资产与打包内容一致，真实运行路径未进入 wheel。

实际 Finder 双击 `stop-dashboard.command` 后原服务退出，双击 `open-dashboard.command` 后原端口服务
通过项目身份验证、连接正常；核对进程拥有独立会话。Terminal 的 UI 控制被工具限制，关闭其窗口的手势
记为 not_run，未用其他 UI 自动化绕过；独立父进程退出/会话验证通过。没有关闭或重启用户整台电脑。

合成浏览器两个受控场景：catchup-failure（45 日空档）与 catchup-limited（400 日空档＋首次模拟失败）。
检查首次打开一次请求、失败后刷新不自动重试、切 30 天保留原断点范围、主动续传成功、原始来源弹窗与 Escape、
周期记录切换、本周进度与记录来源、旧周选择、可选 Journal 空态、366 天跨年范围及重新同步完成。
两个场景各只发生首次条件请求＋一次明确续传/重新同步，导航/刷新未增加额外同步。
桌面默认 1280×720 和窄屏 390×844 检查通过，日期/说明可换行，无页面横向溢出；控制台未见警告或错误。
验收中工具单次等待在模拟同步完成前超时，随后状态读取确认操作成功；未当作产品故障或重复发起同步。
只读合成 DOM/截图，未读真实页面内容；验收页关闭，临时视口重置、服务停止、临时库清理。

`tests/process_recovery_drill.py` 使用六个实际合成子进程，在持久分页断点、入库完成但任务尚未完成两个位置
执行 SIGKILL，验证进程退出释放锁、断点可读、打开不自动重试、原区间/游标/计划续传、完成重放幂等，
并在新进程中模拟 45 日后补取。临时进程全部回收、库全部删除，不使用外部网络。

真实同机恢复通过：停止原服务并持同步锁，正常到期清理和基线视图读取后创建 SQLCipher 受管备份，
使用独立路径对应的 OS Keychain 密钥恢复到私有临时目录，所有健康内容仅在进程内比较。
全部非 managed_backups 表、获知/到期时间、六指标×7/30 天、完整前周/本周及来源页、两窗口恢复回看页均一致。
SQLCipher 完整性/外键、0600 权限、普通 SQLite 拒读、过期快照恢复拒绝通过。
恢复副本初始登记为空；在副本新建备份并删除副本 WHOOP 来源，只有副本自建备份被清理，
原库数据、登记、原有备份及本次源备份内容/身份均不受影响。最后移除本次源备份登记/文件及所有临时 Keychain 项，
确认原库数据、原登记与原密钥保持不变。服务重新启动并保持可用，没有保留额外真实数据副本。

演练发现并修复旧 restore 继承源库 managed_backups 的问题；新增删除来源和到期两项加密回归，
并在恢复目标增加 integrity_check。schema 1 合成兼容、原数据库归属及所有其他表不变。
用户在本批回复 WHOOP App 核对“一致”，范围为恢复回看周期/睡眠关联及部分周回顾来源日期/原值。
不等同于全部历史对账、实际数月离线、整机重启、跨设备密钥恢复或生产灾难恢复验收。
证据见 [F3_2_VALIDATION_2026-09-07.json](../evidence/F3_2_VALIDATION_2026-09-07.json)。

## F3.3 数据稳健性加固

2026-09-07，0.4.2 / schema 3。**612 passed in 6.06s**；Ruff、离线锁文件核查、wheel 构建及源文件/固定
OpenAPI 打包一致性通过。新增 54 项回归覆盖：

- 网络抓取/排队期间跨过到期，清理和判重原子执行，新采集仍可用，旧缓存按期清理。
- 分析计算后并发来源删除、到期、输入哈希变化和 as_of 发布拒绝，已存缓存读取也重核来源。
- sleep/workout UUID 大小写、单批冲突、直接 SourceRecordInput、别名修正及删除时间戳优先。
- schema 2 的真实重开迁移在合成 SQLite 与临时 SQLCipher 均保持原行 ID、哈希、原文、观测和期限，
  规范键碰撞保留全部源历史；受影响分析失效；schema 1→2→3 及旧 schema 2 备份恢复通过。
- 未来新请求拒绝、旧异常成功区间裁剪、时钟回拨、空可信区间和跨月补取。
- 即时页面/完整与进行中周回顾/来源分页不落库，无关 profile/body 不失效显式健康分析；
  205 个显式请求分为 100/100/5，待处理去重，运行中再次修正不漏后续任务。
- 当前修正或删除标记先到期时清理其同身份前驱，不复活旧值；后继非当前历史和其他身份独立保留，
  中间历史到期不误伤有效胜出版本，源规范值、键、缓存、任务与受管备份同步清理。

`tests/process_recovery_drill.py` 再次通过分页后/入库后的 SIGKILL、续传、幂等、锁释放、45 日补取及全部临时清理。
`tests/benchmark_hardening.py --large` 在临时合成库通过：1,000 次查看约 1.82 秒，新增分析/任务/分析文本均为 0；
额外 1 万/10 万/20 万历史版本，页面中位约 2.1/4.8/8.4ms，Python 峰值约 225KB，选中记录恒定、索引命中。
这些为结构压力测试，不等于实际 SQLCipher 或多年真实账户耗时；未采用脆弱的耗时断言。

本机升级：通过既有 Keychain 身份验证停止 0.4.1 服务；在临时加密隔离副本先执行迁移并校验，再以同一时刻升级
原 SQLCipher 库。源连接/版本、观测、活动、分析、任务、同步页/请求、规范键与设置的表签名和隔离结果一致；
原库 schema 3、完整性与外键通过。副本预先清空原备份登记，清理不会影响原受管备份；临时副本已删除，凭据未改。
随后 0.4.2 服务在原 8766 地址启动并打开浏览器；概览 7/30 天、周回顾、恢复回看 7/30 天及连接技术检查通过，
概览确实使用非持久分析。健康内容仅在本机处理，未输出值、实际日期、来源 ID、原文、凭据、DOM 或截图。

本批不新增网络采集机制、后台任务或云资源。历史轮查和上游删除完整传播仍未实现，全部历史与 WHOOP App
逐条对账、跨设备恢复、真实多年压力测试为 not_run。修复前审查及反例保留在 reviews/2026-09-07，
正式修复回归在 tests，不以旧反例的“通过”代表修复通过。

证据见 [F3_3_VALIDATION_2026-09-07.json](../evidence/F3_3_VALIDATION_2026-09-07.json)。
