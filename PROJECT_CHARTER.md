# WHOOP Personal Copilot

WHOOP 负责采集与官方指标，本项目提供可复算趋势、长期个人上下文、训练记录及其他获准数据整合。
WHOOP 是首个数据源。优先使用其已有 API、导出、指标与文档；项目的价值放在来源追踪、
跨来源扩展与个人工作流程，避免复制 WHOOP 的评分算法或未经需要重写通用基础设施。

2026-09-05 用户确认从当前审查材料新建应用。F0 交付是本地合成环境的小型纵向闭环：
WHOOP v2 形状的恢复/周期数据与一个手工 CSV 指标、版本化存储、确定性趋势和 EvidenceBundle、
本地训练草稿/审批/提交、修正重算、CLI 与 MCP 协议验证。

当前实现默认单用户、模块化单体、SQLite。用户随后批准 F1：官方导出显式映射、
WHOOP v2 OAuth 与六类只读资源同步、真实环境 SQLCipher 保护与有期限的本地存储。
代码以合成和模拟 HTTP 验证；实际 WHOOP 账户、OS Keychain 和真实导出联调另记状态。
2026-09-07 用户批准 F2 本地 Dashboard 第一版：最近官方恢复/睡眠/周期负荷，7/30 天六项趋势，
来源详情、手动同步、连接与断点状态，以及真实刷新和 30 天同步验证。
本机浏览器读取同一 SQLCipher 库；默认仍是合成环境，不增加模型外发、远程访问或云资源。
F2.1 经用户批准完善请求范围/实际记录覆盖和空态说明，并以合成场景完成浏览器交互与窄屏验收。
实际佩戴时长不由请求窗口或记录日期推断；范围、条数、有效样本与同步状态分别展示。
F2.2 经批准增加打开时按需更新、请求新鲜度和主动恢复，继续复用既有同步服务。
F2.3 经批准复用 WHOOP 官方 Journal 导出及既有 CSV/ZIP 导入器，增加本地只读日志时间线。
明确映射日期、问答和本人归属后，与该日官方恢复/睡眠/周期负荷并列，并可查看各自来源。
当前代码与合成验收已完成，实际 Journal 文件尚未取得；真实表头、日期语义和导入兼容性仍待核对。
用户随后修订方向：日常使用不应依赖定期导出/导入。Journal 导入降为可选历史补充，
真实文件联调暂缓，不阻塞后续开发；日常主线使用官方 API 可自动获取的数据。
F2.4 经用户批准增加本地每日摘要与近期变化，复用已有官方概览、注册统计及来源详情，
无需新增输入或模型。按各自记录日期和评分状态呈现，未结束周期明确提示；短历史直接使用现有样本。
F2.5 经后续授权增加本地周回顾，按 UTC 日历周查看六项既有指标与前周的描述比较。
默认上个完整周，本周仅显示进度；最多选择最近 12 周中的本机保留记录，数据不足不要求补录。
沿用来源版本与保留期限，周回顾随记录修正而更新；永久报告归档和后台周报仍属后续范围。
F2.6 按后续继续指令实现恢复回看，按官方 ID 并列同一周期的恢复、关联睡眠与周期负荷，
补充展示已规范化的睡眠效率、一致性和呼吸率。缺项或版本矛盾明确提示，不按日期强配或推断原因。
F3.1 经用户批准完善一键启动/停止与长间隔补取，适应每天关机、周末不开机的使用方式。
打开时按需更新，合并成功请求的区间补取空档，并回查近期评分/修正；自动回查最多 366 天。
定时同步暂缓，不增加登录项、云服务或日常手工输入。
F3.2 经用户批准完成第一版使用验收与同机隔离恢复演练，用户反馈新增视图抽查一致。
修复恢复副本的备份清理归属，验证重加密恢复、视图一致、期限与临时数据清理。
整机关机/开机、跨设备密钥恢复和完整历史对账仍为独立后续验收。
用户事实与模型假设的完整管理、模型调用、教练、通知与部署属于后续批次。

F3.3 经独立审查后获准进行数据稳健性加固，修复到期/去重、分析删除传播、UUID 身份、未来同步窗口、
日常缓存增长和历史查询成本；继续使用同一 Python 服务与 SQLCipher。schema 3 保留原文及版本证据身份。
本机迁移和服务重启已完成，历史轮查/上游删除完整对账不以这批修复冒充已交付。

成功标准：能运行、能解释数字来源、能保留历史并复算、能阻止未经批准或冲突的提交；
增加一个合理来源或指标主要需要新增适配器/定义，而不需要重写数据底座。
