# 工作路线

**V1 已完成，当前技术版本为 0.4.2 / schema 3。** 下文保留各阶段范围与验收边界；
后续事项按实际需求另行授权，具体通过结果见 [实施状态](STATUS.md)。

## F0 · 合成基础闭环

实现及验证结果见 `STATUS.md`。交付范围包含多来源契约、源版本和获知时间、可复算趋势、
EvidenceBundle、受控本地训练计划、持久任务、CLI、MCP stdio 和 SQLite 备份恢复。

## F1 · 官方资源复用与可靠接入

先评估 WHOOP 官方导出作为用户主动导入和历史回填路径，同时规划官方 v2 OAuth 接入。
固定并校验官方 OpenAPI 契约；按资源逐项实现 cycle/recovery/sleep/workout/profile/body measurement，
沿用官方分页、令牌轮换、限流与 webhook 机制，不重复开发官方已有评分。
真实数据启用前完成适用授权、字段覆盖、加密、保留与删除传播验证。

当前已实现 OAuth/六资源同步、导出映射、断点和加密本地存储，并通过合成验证；
2026-09-07 已完成应用创建后的首次真实 OAuth、OS Keychain 和六资源小窗口同步/本地复算；
随后真实刷新与 30 天窗口同步已通过；撤销、实际 CSV 样本和完整历史对账仍待完成。
创建步骤见 [接入指南](docs/WHOOP_SETUP.md)。Webhook、自动调度和生产验证保持后续工作项。

## F2 · 本地日常界面

按用户于 2026-09-05 确认的方向，Dashboard 先在本地运行，云服务器部署以后再考虑；
沿用 Python 共享服务与 SQLite，保留整套迁移能力，见 [D11](docs/DECISIONS.md#d11dashboard-先本地运行云部署后续再考虑)。
2026-09-07 已实现第一版：最近官方概览、六项 7/30 天趋势、来源详情、手动同步和连接/断点状态。
看板复用 CLI/MCP 的计算服务；HTTP 仅本机开放，MCP 仍保持合成环境。完整验证见 STATUS.md。
F2.1 已完善记录覆盖/请求范围、无记录及未评分空态，并通过合成浏览器交互与窄屏验收。
真实 WHOOP App 抽查已由用户反馈“一致”；全部历史逐条对账仍独立验收，不要求用尚未存在的历史填满请求窗口。
F2.2 增加新鲜度提示、打开时的一次按需更新和失败恢复；已有断点需主动继续或重新同步。
该批不包含页面停留时的定时采集、常驻调度、通知或云部署。

F2.3 已实现官方 Journal CSV/ZIP 显式映射与本地只读时间线，复用既有导入、来源版本、
加密存储及官方指标视图；合成验收通过。实际文件尚未下载，真实映射与导入验收仍为 not_run，
仅在用户以后主动需要历史补充时核对日期、问答、稳定身份和账户归属。操作见 [Journal 接入](docs/JOURNAL.md)。
Journal 通过主动导入更新，API 同步不会更新日志；这一批不实现日志编辑或行为影响分析。

用户随后明确指出定期手动导出/导入违背日常使用目标。F2.3 导入因此降为可选历史补充，
实际 Journal 验收暂缓，不再等待或要求下载文件，不作为主线依赖。
2026-09-07 复核公开个人 API 与 webhook，未发现 Journal 读取端点或日志事件，见
[官方资源复核](docs/WHOOP_RESOURCES.md#2026-09-07journal-日常接入复核)。
自动处理已下载文件仍需要用户反复生成导出，不满足端到端自动接入目标。

F2.4 经用户批准，已以官方 API 已有的恢复、睡眠和负荷数据生成本地每日摘要及短期变化，
复用现有评分、统计、来源证据与打开时按需更新；缺少 Journal 或不足 30 天不阻塞使用。
摘要与三卡、详细趋势共享同一来源快照，各项日期独立保留；前后半窗口每段至少两条有效观测才比较。
不推断未提供的生活行为或行为原因，不把差值称作改善/恶化或官方新评分。验证范围见 STATUS.md。

F2.5 经后续授权已增加本地周回顾：默认上个完整 UTC 日历周，复用六项官方指标和注册均值，
与前一完整周比较；本周仅显示进度。最近 12 个日历周可选择，按本机当前保留记录重算，
短历史显示实际样本，记录与来源可分页核查。周回顾不依赖 Journal 或新增日常操作，
F2.5 交付时仅更新最近 30 天；F3.1 已将按需同步扩展为有限历史补取，见下文。

F2.6 已推进可追溯的恢复回看：按官方 cycle_id / sleep_id，把本周期恢复、关联睡眠和周期负荷
并列展示。复用已有规范观测与来源详情，增加已有睡眠效率、一致性和呼吸率的只读显示；
7/30 天筛选周期起点，每页 10 个周期，关联睡眠允许跨筛选边界。
缺项、版本矛盾和关联冲突明确提示，不以较晚小睡或邻近日历日期替代；不推断因果或生成新评分。
该能力无需新增填写、Journal 导出、权限或网络接入，具体验证见 STATUS.md。

后续按用户需求完善个人上下文、更丰富的注册分析、永久报告归档/后台周报与反馈；这些仍未交付。
结合 WHOOP 已有 Coach 功能，优先做可追溯追问、个人记录和跨来源分析。
模型供应商与外发范围按用户批准配置，核心查询在无模型时继续可用。

## F3 · 自动化与上线

### F3.1 · 启动体验与长间隔补取

用户说明电脑每天关机、周末可能不开机，明确批准本批并暂缓后台定时同步。
一键启动后打开本机看板，重复启动复用已验证的同项目服务；提供独立停止入口，无需保留终端。
打开时复用原条件检查与 SyncService，按成功请求区间补取长时间未打开产生的空档；
近期窄窗口请求不能跳过已知旧空档，晚完成的旧断点也不能冒充查到了今天。
重查起点附近 7 个 UTC 日历日，最多回查 366 天；起点记录已过期或缺失时同样进行有限回查。
范围在界面说明，不能承诺更早历史、所有旧修正或删除都已获取。现有评分、分页、OAuth、
加密存储和主动断点恢复继续复用，不增加每日操作或 Journal 依赖。实际验证见 STATUS.md。

### F3.2 · 第一版使用验收与恢复演练

2026-09-07 已完成 Finder 双击启动/停止、合成页面操作与窄屏、实际进程异常中断/续传，以及
真实 SQLCipher 备份→隔离重加密恢复→视图对比→清理演练；用户反馈本批官方 App 抽查一致。
验收发现恢复副本继承原库备份清理登记，已修复为每个恢复库只管理自身后续新建的备份。
临时真实副本、登记和临时 Keychain 项全部清理，原库数据、备份及原密钥验证未变。
当前为同一台电脑上的恢复验证；整机重启、跨设备密钥恢复、全部历史对账及生产灾难恢复仍未实测。
具体通过范围与限制见 STATUS.md 和 docs/ACCEPTANCE.md。

### F3.3 · 数据稳健性加固与 V1 收尾

0.4.2 / schema 3 已修复到期与去重竞态、删除后分析重写、UUID 别名、未来同步窗口、
日常页面缓存增长与历史查询成本。来源原文、原始哈希及版本 ID 保留；
修正/删除/到期继续遵守既有保留策略，日常视图即时计算，显式分析才保存证据。
合成回归、隔离加密迁移与本机升级已完成；历史轮查和上游删除完整对账仍是独立能力边界。
V1 收尾将历史审查材料统一归档至 `reviews/`，保留当前文档与验收证据，整理运行入口。

### 后续按需求推进

后台定时同步暂缓。通知、可选 webhook、云部署、跨设备恢复演练、完整真实数据对账和生产验收
仍是独立后续事项，已有合成测试不能替代这些验证。
